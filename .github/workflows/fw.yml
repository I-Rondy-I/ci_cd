name: First Workflow
on: [workflow_dispatch]
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello GH!
      - name: Print a few lines
        run: |
          echo First Line
          echo Second Line
